" See ~/.vim/plugin/*.vim for initialization code.
" See ~/.vim/pack/* for plugins and colors (:help packages)

" Setting this under .vim/plugin does not seem to have an effect,
" must be done here. Ctrl+C+C will run the command under cursor inside vim.
let g:slime_target="vimterminal"

